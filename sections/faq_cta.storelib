<template>
<section class="s" data-scheme="{{ settings.color_scheme }}" style="--s-bg: {{ scheme.bg }}; --s-text: {{ scheme.text }}; --s-heading: {{ scheme.heading }}; --s-para: {{ scheme.paragraph }}; --s-border: {{ scheme.border }}; --s-accent: {{ scheme.accent }}; --s-btn: {{ scheme.primary_btn_bg }}; --s-btn-text: {{ scheme.primary_btn_text }}; --s-btn2: {{ scheme.secondary_btn_bg }}; --s-btn2-text: {{ scheme.secondary_btn_text }}; --s-btn2-border: {{ scheme.secondary_btn_border }}; --s-radius: {{ theme.radius.medium }}px; --s-pt: {{ settings.padding_top }}px; --s-pb: {{ settings.padding_bottom }}px;">
  <div class="s__in s__in--{{ settings.max_width }}">
    {% if settings.eyebrow or settings.heading or settings.subheading %}
      <div class="s__head {% if settings.align == 'center' %}s__head--center{% endif %} s__headblock">
        {% if settings.eyebrow %}<p class="s__eyebrow">{{ settings.eyebrow }}</p>{% endif %}
        {% if settings.heading %}<h2 class="s__h">{{ settings.heading }}</h2>{% endif %}
        {% if settings.subheading %}<p class="s__sub">{{ settings.subheading }}</p>{% endif %}
      </div>
    {% endif %}
    {% if blocks.size == 0 %}
      {% if page.in_editor %}<p class="empty">Add questions in the sidebar</p>{% endif %}
    {% else %}
      <div class="qa__list">
        {% for block in blocks %}

          <details class="qa" data-block-id="{{ block.id }}"{% if block.settings.open %} open{% endif %}>
            <summary class="qa__q">
              <span>{{ block.settings.question }}</span>
              <span class="qa__mark" aria-hidden="true"></span>
            </summary>
            <div class="qa__a rt">{{ block.settings.answer | raw }}</div>
          </details>
        {% endfor %}
      </div>
    {% endif %}
    <div class="fend">
      {% if settings.cta_heading %}<p class="fend__h">{{ settings.cta_heading }}</p>{% endif %}
      {% if settings.cta_text %}<p class="fend__t">{{ settings.cta_text }}</p>{% endif %}
      {% if settings.button_text %}<a class="s__btn s__btn--primary" href="{{ settings.button_url | default: '#' | url }}">{{ settings.button_text }}</a>{% endif %}
    </div>
  </div>
</section>
</template>

<style scoped>
.s { container-type: inline-size; background: var(--s-bg); color: var(--s-text); }
.s__in {
  margin: 0 auto;
  padding: calc(var(--s-pt) * var(--theme-spacing-scale, 1)) var(--theme-padding-x, 24px) calc(var(--s-pb) * var(--theme-spacing-scale, 1));
}
.s__in--narrow { max-width: 720px; }
.s__in--medium { max-width: 960px; }
.s__in--wide { max-width: var(--theme-page-width, 1280px); }
.s__in--full { max-width: none; }
.s__eyebrow { margin: 0 0 12px; font-size: 12px; font-weight: 600; letter-spacing: 0.14em; text-transform: uppercase; color: var(--s-accent); }
.s__h {
  margin: 0;
  /* The section's heading style: Heading 2 unless the section picked
     another, set by the theme stylesheet on the section root. */
  font-size: var(--section-heading-size, 32px);
  line-height: var(--section-heading-line-height, 1.15);
  font-weight: var(--section-heading-weight, 700);
  color: var(--s-heading);
  letter-spacing: var(--section-heading-tracking, -0.01em);
  text-transform: var(--section-heading-transform, none);
  /* The theme's font when the page sets one, the token otherwise, and a real
     stack either way: a bare family name falls back to the browser's serif. */
  font-family: var(--theme-font-heading, {{ theme.typography.heading }}), ui-sans-serif, system-ui, sans-serif;
}
.s__sub { margin: 14px 0 0; font-size: var(--section-text-size, 17px); line-height: var(--section-text-line-height, 1.6); color: var(--s-para); }
.s__head { max-width: 44rem; }
.s__head--center { margin-inline: auto; text-align: center; }
.s__btns { display: flex; flex-wrap: wrap; gap: 12px; margin-top: 28px; }
.s__head--center .s__btns { justify-content: center; }
.s__btn {
  display: inline-flex; align-items: center; justify-content: center;
  padding: 13px 24px; font-size: 15px; font-weight: 600; text-decoration: none;
  border-radius: var(--s-radius); border: 1px solid transparent; transition: opacity 150ms ease;
}
.s__btn:hover { opacity: 0.88; }
.s__btn--primary { background: var(--s-btn); color: var(--s-btn-text); border-color: var(--s-btn); }
/* The scheme's OWN secondary-button colours, not a stand-in for them.
   This said `transparent / var(--s-heading) / var(--s-border)`, so the
   two Secondary button controls in the section panel changed nothing at
   all, and on a light scheme the outline was drawn in the faint divider
   colour instead of the dark one the scheme asks for. */
.s__btn--secondary { background: var(--s-btn2); color: var(--s-btn2-text); border-color: var(--s-btn2-border); }
.s__note { margin: 16px 0 0; font-size: 13px; color: var(--s-para); opacity: 0.8; }

.s__headblock { margin-bottom: 40px; }

.qa { border-bottom: 1px solid var(--s-border); }
.qa:first-of-type { border-top: 1px solid var(--s-border); }
.qa__q {
  display: flex; align-items: center; justify-content: space-between; gap: 16px;
  padding: 18px 2px; cursor: pointer; list-style: none;
  font-size: 15px; font-weight: 600; color: var(--s-heading);
}
.qa__q::-webkit-details-marker { display: none; }
.qa__mark { position: relative; flex: none; width: 14px; height: 14px; }
.qa__mark::before, .qa__mark::after {
  content: ""; position: absolute; inset: 0; margin: auto;
  background: var(--s-heading); transition: opacity 150ms ease, rotate 200ms ease;
}
.qa__mark::before { width: 14px; height: 2px; }
.qa__mark::after { width: 2px; height: 14px; }
.qa[open] .qa__mark::after { opacity: 0; rotate: 90deg; }
.qa__a { padding: 0 2px 20px; font-size: 15px; line-height: 1.65; color: var(--s-para); }
.qa__a > :first-child { margin-top: 0; } .qa__a > :last-child { margin-bottom: 0; }
.qa__a p { margin: 0 0 0.9em; }
.qa__a a { color: var(--s-heading); text-decoration: underline; text-underline-offset: 3px; }
.empty { margin: 0; text-align: center; font-size: 14px; color: var(--s-para); opacity: 0.55; }

.fend { margin-top: 36px; padding: 26px; border: 1px solid var(--s-border); border-radius: var(--s-radius); text-align: center; }
.fend__h { margin: 0; font-size: 16px; font-weight: 600; color: var(--s-heading); }
.fend__t { margin: 6px 0 18px; font-size: 14px; line-height: 1.6; color: var(--s-para); }
</style>

<schema>
{
  "schema_version": 1,
  "type": "faq_cta",
  "name": "FAQ + CTA",
  "category": "faq",
  "description": "Questions, then somewhere to go if the answer is not there.",
  "version": "1.0.0",
  "icon": "help-circle",
  "settings": [
    {
      "type": "header",
      "id": "hdr_content",
      "label": "Content"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": ""
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Questions"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Supporting line",
      "default": ""
    },
    {
      "type": "radio",
      "id": "align",
      "label": "Heading sits",
      "default": "center",
      "options": [
        {
          "label": "Left",
          "value": "left"
        },
        {
          "label": "Centre",
          "value": "center"
        }
      ]
    },
    {
      "type": "header",
      "id": "hdr_cta",
      "label": "At the end"
    },
    {
      "type": "text",
      "id": "cta_heading",
      "label": "Heading",
      "default": "Still stuck?"
    },
    {
      "type": "textarea",
      "id": "cta_text",
      "label": "Text",
      "default": "Send us a message and you will hear back the same day."
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button",
      "default": "Get in touch"
    },
    {
      "type": "url",
      "id": "button_url",
      "label": "Button link",
      "default": "/contact"
    },
    {
      "type": "header",
      "id": "hdr_layout",
      "label": "Layout"
    },
    {
      "type": "select",
      "id": "max_width",
      "label": "Content width",
      "default": "narrow",
      "options": [
        {
          "label": "Narrow",
          "value": "narrow"
        },
        {
          "label": "Medium",
          "value": "medium"
        },
        {
          "label": "Wide",
          "value": "wide"
        },
        {
          "label": "Full",
          "value": "full"
        }
      ]
    },
    {
      "type": "header",
      "id": "hdr_style",
      "label": "Style"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Colour scheme",
      "default": "white"
    },
    {
      "type": "header",
      "id": "hdr_spacing",
      "label": "Spacing"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Top padding",
      "default": 72,
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Bottom padding",
      "default": 72,
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px"
    }
  ],
  "blocks": [
    {
      "type": "qa",
      "name": "Question",
      "settings": [
        {
          "type": "text",
          "id": "question",
          "label": "Question",
          "default": "How long does delivery take?"
        },
        {
          "type": "richtext",
          "id": "answer",
          "label": "Answer",
          "default": "<p>Two to four working days once it leaves the studio, and you get a tracking link the moment it does.</p>"
        },
        {
          "type": "toggle",
          "id": "open",
          "label": "Open to begin with",
          "default": false
        }
      ]
    }
  ],
  "max_blocks": 30,
  "presets": [
    {
      "name": "FAQ + CTA",
      "settings": {},
      "blocks": [
        {
          "id": "b1",
          "type": "qa",
          "settings": {
            "question": "How long does delivery take?",
            "answer": "<p>Two to four working days once it leaves the studio, and you get a tracking link the moment it does.</p>",
            "open": true
          }
        },
        {
          "id": "b2",
          "type": "qa",
          "settings": {
            "question": "Can I send something back?",
            "answer": "<p>Thirty days, unworn, and we pay the return postage.</p>"
          }
        },
        {
          "id": "b3",
          "type": "qa",
          "settings": {
            "question": "Do you ship abroad?",
            "answer": "<p>Yes, everywhere except a short list of places the courier will not go.</p>"
          }
        }
      ]
    }
  ]
}
</schema>
