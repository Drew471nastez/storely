<template>
<section class="s hero2 hero2--{{ settings.image_side }}" data-scheme="{{ settings.color_scheme }}" style="--s-bg: {{ scheme.bg }}; --s-text: {{ scheme.text }}; --s-heading: {{ scheme.heading }}; --s-para: {{ scheme.paragraph }}; --s-border: {{ scheme.border }}; --s-accent: {{ scheme.accent }}; --s-btn: {{ scheme.primary_btn_bg }}; --s-btn-text: {{ scheme.primary_btn_text }}; --s-btn2: {{ scheme.secondary_btn_bg }}; --s-btn2-text: {{ scheme.secondary_btn_text }}; --s-btn2-border: {{ scheme.secondary_btn_border }}; --s-radius: {{ theme.radius.medium }}px; --s-pt: {{ settings.padding_top }}px; --s-pb: {{ settings.padding_bottom }}px;">
  <div class="s__in s__in--{{ settings.max_width }} hero2__in">
    <div class="s__head hero2__copy">
      {% if settings.eyebrow %}<p class="s__eyebrow">{{ settings.eyebrow }}</p>{% endif %}
      {% if settings.heading %}<h1 class="s__h">{{ settings.heading }}</h1>{% endif %}
      {% if settings.subheading %}<p class="s__sub">{{ settings.subheading }}</p>{% endif %}
      {% if settings.list_1 or settings.list_2 or settings.list_3 %}
        <ul class="hero2__list">
          {% if settings.list_1 %}<li>{{ settings.list_1 }}</li>{% endif %}
          {% if settings.list_2 %}<li>{{ settings.list_2 }}</li>{% endif %}
          {% if settings.list_3 %}<li>{{ settings.list_3 }}</li>{% endif %}
        </ul>
      {% endif %}
      {% if settings.button_text or settings.button2_text %}
        <div class="s__btns">
          {% if settings.button_text %}<a class="s__btn s__btn--primary" href="{{ settings.button_url | default: '#' | url }}">{{ settings.button_text }}</a>{% endif %}
          {% if settings.button2_text %}<a class="s__btn s__btn--secondary" href="{{ settings.button2_url | default: '#' | url }}">{{ settings.button2_text }}</a>{% endif %}
        </div>
      {% endif %}
      {% if settings.note %}<p class="s__note">{{ settings.note }}</p>{% endif %}
    </div>
    <div class="hero2__media hero2__media--{{ settings.image_shape }}">
      {% if settings.image %}
        <img src="{{ settings.image | asset }}" alt="{{ settings.image_alt }}" loading="eager" fetchpriority="high" decoding="async">
      {% else %}
        <div class="hero2__placeholder" aria-hidden="true"></div>
      {% endif %}
    </div>
  </div>
</section>
</template>

<style scoped>
.s { container-type: inline-size; background: var(--s-bg); color: var(--s-text); }
.s__in {
  margin: 0 auto;
  padding: calc(var(--s-pt) * var(--theme-spacing-scale, 1)) var(--theme-padding-x, 24px) calc(var(--s-pb) * var(--theme-spacing-scale, 1));
}
.s__in--narrow { max-width: 720px; }
.s__in--medium { max-width: 960px; }
.s__in--wide { max-width: var(--theme-page-width, 1280px); }
.s__in--full { max-width: none; }
.s__eyebrow { margin: 0 0 12px; font-size: 12px; font-weight: 600; letter-spacing: 0.14em; text-transform: uppercase; color: var(--s-accent); }
.s__h {
  margin: 0;
  /* The section's heading style: Heading 2 unless the section picked
     another, set by the theme stylesheet on the section root. */
  font-size: var(--section-heading-size, 32px);
  line-height: var(--section-heading-line-height, 1.15);
  font-weight: var(--section-heading-weight, 700);
  color: var(--s-heading);
  letter-spacing: var(--section-heading-tracking, -0.01em);
  text-transform: var(--section-heading-transform, none);
  /* The theme's font when the page sets one, the token otherwise, and a real
     stack either way: a bare family name falls back to the browser's serif. */
  font-family: var(--theme-font-heading, {{ theme.typography.heading }}), ui-sans-serif, system-ui, sans-serif;
}
.s__sub { margin: 14px 0 0; font-size: var(--section-text-size, 17px); line-height: var(--section-text-line-height, 1.6); color: var(--s-para); }
.s__head { max-width: 44rem; }
.s__head--center { margin-inline: auto; text-align: center; }
.s__btns { display: flex; flex-wrap: wrap; gap: 12px; margin-top: 28px; }
.s__head--center .s__btns { justify-content: center; }
.s__btn {
  display: inline-flex; align-items: center; justify-content: center;
  padding: 13px 24px; font-size: 15px; font-weight: 600; text-decoration: none;
  border-radius: var(--s-radius); border: 1px solid transparent; transition: opacity 150ms ease;
}
.s__btn:hover { opacity: 0.88; }
.s__btn--primary { background: var(--s-btn); color: var(--s-btn-text); border-color: var(--s-btn); }
/* The scheme's OWN secondary-button colours, not a stand-in for them.
   This said `transparent / var(--s-heading) / var(--s-border)`, so the
   two Secondary button controls in the section panel changed nothing at
   all, and on a light scheme the outline was drawn in the faint divider
   colour instead of the dark one the scheme asks for. */
.s__btn--secondary { background: var(--s-btn2); color: var(--s-btn2-text); border-color: var(--s-btn2-border); }
.s__note { margin: 16px 0 0; font-size: 13px; color: var(--s-para); opacity: 0.8; }

.hero2__in { display: grid; gap: 40px; align-items: center; grid-template-columns: 1fr; }
@container (min-width: 860px) {
  .hero2__in { grid-template-columns: 1fr 1fr; gap: 56px; }
  .hero2--left .hero2__copy { order: 2; }
}
.hero2__copy { max-width: 34rem; }
.hero2__list { margin: 22px 0 0; padding: 0; list-style: none; display: grid; gap: 10px; }
.hero2__list li { position: relative; padding-left: 26px; font-size: 15px; line-height: 1.5; color: var(--s-para); }
.hero2__list li::before {
  content: ""; position: absolute; left: 0; top: 7px; width: 14px; height: 8px;
  border-left: 2px solid var(--s-accent); border-bottom: 2px solid var(--s-accent); rotate: -45deg;
}
.hero2__media { overflow: hidden; border-radius: var(--s-radius); }
.hero2__media--round { border-radius: 24px; }
.hero2__media--square { border-radius: 0; }
.hero2__media img { display: block; width: 100%; height: 100%; object-fit: cover; aspect-ratio: 4 / 3; }
.hero2__placeholder { width: 100%; aspect-ratio: 4 / 3; background: var(--s-border); }
</style>

<schema>
{
  "schema_version": 1,
  "type": "hero_split",
  "name": "Split Hero",
  "category": "hero",
  "description": "Words on one side, a picture on the other.",
  "version": "1.0.0",
  "icon": "columns",
  "settings": [
    {
      "type": "header",
      "id": "hdr_content",
      "label": "Content"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": ""
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Made by hand, sent the same week"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Supporting line",
      "default": "Small batches, honest materials and a note in every parcel."
    },
    {
      "type": "text",
      "id": "list_1",
      "label": "Point 1",
      "default": ""
    },
    {
      "type": "text",
      "id": "list_2",
      "label": "Point 2",
      "default": ""
    },
    {
      "type": "text",
      "id": "list_3",
      "label": "Point 3",
      "default": ""
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button",
      "default": "See what's in stock"
    },
    {
      "type": "url",
      "id": "button_url",
      "label": "Button link",
      "default": "/products"
    },
    {
      "type": "text",
      "id": "button2_text",
      "label": "Second button",
      "default": ""
    },
    {
      "type": "url",
      "id": "button2_url",
      "label": "Second button link",
      "default": ""
    },
    {
      "type": "text",
      "id": "note",
      "label": "Small print",
      "default": ""
    },
    {
      "type": "header",
      "id": "hdr_image",
      "label": "Image"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image",
      "default": ""
    },
    {
      "type": "text",
      "id": "image_alt",
      "label": "Image description",
      "default": "",
      "info": "Read aloud to people who cannot see it."
    },
    {
      "type": "radio",
      "id": "image_side",
      "label": "Image on",
      "default": "right",
      "options": [
        {
          "label": "Left",
          "value": "left"
        },
        {
          "label": "Right",
          "value": "right"
        }
      ]
    },
    {
      "type": "select",
      "id": "image_shape",
      "label": "Corners",
      "default": "default",
      "options": [
        {
          "label": "Theme",
          "value": "default"
        },
        {
          "label": "Rounded",
          "value": "round"
        },
        {
          "label": "Square",
          "value": "square"
        }
      ]
    },
    {
      "type": "header",
      "id": "hdr_layout",
      "label": "Layout"
    },
    {
      "type": "select",
      "id": "max_width",
      "label": "Content width",
      "default": "wide",
      "options": [
        {
          "label": "Narrow",
          "value": "narrow"
        },
        {
          "label": "Medium",
          "value": "medium"
        },
        {
          "label": "Wide",
          "value": "wide"
        },
        {
          "label": "Full",
          "value": "full"
        }
      ]
    },
    {
      "type": "header",
      "id": "hdr_style",
      "label": "Style"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Colour scheme",
      "default": "white"
    },
    {
      "type": "header",
      "id": "hdr_spacing",
      "label": "Spacing"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Top padding",
      "default": 88,
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Bottom padding",
      "default": 88,
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px"
    }
  ],
  "presets": [
    {
      "name": "Split Hero"
    },
    {
      "name": "Split Hero, image left",
      "settings": {
        "image_side": "left"
      }
    }
  ]
}
</schema>
