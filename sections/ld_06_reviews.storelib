<template>
<section class="ldr" data-scheme="{{ settings.color_scheme }}" style="--bg:{{ settings.background }};--ink:{{ settings.text }};--muted:{{ settings.muted }};--line:{{ settings.border }};">
  <div class="ldr__in"><h2>No client reviews yet</h2><p>{{ settings.review_text }}</p></div>
</section>
</template>

<style scoped>
.ldr { min-height: 300px; border-top: 1px solid var(--line); border-bottom: 1px solid var(--line); background: var(--bg); color: var(--ink); font-family: Arial, Helvetica, sans-serif; }
.ldr__in { display: grid; grid-template-columns: 1fr 1.15fr; width: min(1180px, calc(100% - 64px)); min-height: 300px; margin: 0 auto; align-items: center; gap: 110px; }
.ldr h2 { margin: 0; font-family: "Bodoni 72", Didot, Georgia, serif; font-size: 52px; font-weight: 400; letter-spacing: -2.2px; line-height: 1; }
.ldr p { max-width: 560px; margin: 0; color: var(--muted); font-size: 14px; line-height: 1.6; }
@container (max-width: 820px) { .ldr__in { grid-template-columns: 1fr; width: calc(100% - 32px); min-height: 390px; padding: 72px 0; gap: 28px; } .ldr h2 { font-size: 44px; } .ldr p { font-size: 15px; } }
@keyframes ldr-reveal { from { opacity: .15; transform: translateY(22px); } to { opacity: 1; transform: translateY(0); } }
@supports (animation-timeline: view()) { .ldr__in { animation: ldr-reveal both cubic-bezier(.22,.61,.36,1); animation-timeline: view(); animation-range: entry 5% cover 28%; } }
@media (prefers-reduced-motion: reduce) { .ldr__in { animation: none; } }
</style>

<schema>
{
  "schema_version": 1,
  "type": "ld_06_reviews",
  "name": "LD 06 — Reviews",
  "category": "custom",
  "settings": [
    { "type": "textarea", "id": "review_text", "label": "Text", "default": "You can still read my work before paying. Start with two free posts for ghostwriting or a free brand check for SEO + GEO/AEO." },
    { "type": "color_scheme", "id": "color_scheme", "label": "Base colour scheme", "default": "white" },
    { "type": "color", "id": "background", "label": "Background", "default": "#FFFFFF" },
    { "type": "color", "id": "text", "label": "Text", "default": "#1A1D24" },
    { "type": "color", "id": "muted", "label": "Muted text", "default": "#4E5156" },
    { "type": "color", "id": "border", "label": "Borders", "default": "#BDC0C5" }
  ],
  "presets": [{ "name": "LD 06 — Reviews" }]
}
</schema>
