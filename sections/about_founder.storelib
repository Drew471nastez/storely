<template>
<section class="s" data-scheme="{{ settings.color_scheme }}" style="--s-bg: {{ scheme.bg }}; --s-text: {{ scheme.text }}; --s-heading: {{ scheme.heading }}; --s-para: {{ scheme.paragraph }}; --s-border: {{ scheme.border }}; --s-accent: {{ scheme.accent }}; --s-btn: {{ scheme.primary_btn_bg }}; --s-btn-text: {{ scheme.primary_btn_text }}; --s-btn2: {{ scheme.secondary_btn_bg }}; --s-btn2-text: {{ scheme.secondary_btn_text }}; --s-btn2-border: {{ scheme.secondary_btn_border }}; --s-radius: {{ theme.radius.medium }}px; --s-pt: {{ settings.padding_top }}px; --s-pb: {{ settings.padding_bottom }}px;">
  <div class="s__in s__in--{{ settings.max_width }} fo__in">
    {% if settings.image %}<img class="fo__photo" src="{{ settings.image | asset }}" alt="{{ settings.name }}" loading="lazy" decoding="async">{% endif %}
    {% if settings.eyebrow %}<p class="s__eyebrow">{{ settings.eyebrow }}</p>{% endif %}
    {% if settings.heading %}<h2 class="s__h fo__h">{{ settings.heading }}</h2>{% endif %}
    {% if settings.body %}<div class="rt fo__body">{{ settings.body | raw }}</div>{% endif %}
    {% if settings.signature %}<img class="fo__sign" src="{{ settings.signature | asset }}" alt="" loading="lazy">{% endif %}
    {% if settings.name %}
      <p class="fo__by"><span class="fo__name">{{ settings.name }}</span>{% if settings.role %}<span class="fo__role">{{ settings.role }}</span>{% endif %}</p>
    {% endif %}
  </div>
</section>
</template>

<style scoped>
.s { container-type: inline-size; background: var(--s-bg); color: var(--s-text); }
.s__in {
  margin: 0 auto;
  padding: calc(var(--s-pt) * var(--theme-spacing-scale, 1)) var(--theme-padding-x, 24px) calc(var(--s-pb) * var(--theme-spacing-scale, 1));
}
.s__in--narrow { max-width: 720px; }
.s__in--medium { max-width: 960px; }
.s__in--wide { max-width: var(--theme-page-width, 1280px); }
.s__in--full { max-width: none; }
.s__eyebrow { margin: 0 0 12px; font-size: 12px; font-weight: 600; letter-spacing: 0.14em; text-transform: uppercase; color: var(--s-accent); }
.s__h {
  margin: 0;
  /* The section's heading style: Heading 2 unless the section picked
     another, set by the theme stylesheet on the section root. */
  font-size: var(--section-heading-size, 32px);
  line-height: var(--section-heading-line-height, 1.15);
  font-weight: var(--section-heading-weight, 700);
  color: var(--s-heading);
  letter-spacing: var(--section-heading-tracking, -0.01em);
  text-transform: var(--section-heading-transform, none);
  /* The theme's font when the page sets one, the token otherwise, and a real
     stack either way: a bare family name falls back to the browser's serif. */
  font-family: var(--theme-font-heading, {{ theme.typography.heading }}), ui-sans-serif, system-ui, sans-serif;
}
.s__sub { margin: 14px 0 0; font-size: var(--section-text-size, 17px); line-height: var(--section-text-line-height, 1.6); color: var(--s-para); }
.s__head { max-width: 44rem; }
.s__head--center { margin-inline: auto; text-align: center; }
.s__btns { display: flex; flex-wrap: wrap; gap: 12px; margin-top: 28px; }
.s__head--center .s__btns { justify-content: center; }
.s__btn {
  display: inline-flex; align-items: center; justify-content: center;
  padding: 13px 24px; font-size: 15px; font-weight: 600; text-decoration: none;
  border-radius: var(--s-radius); border: 1px solid transparent; transition: opacity 150ms ease;
}
.s__btn:hover { opacity: 0.88; }
.s__btn--primary { background: var(--s-btn); color: var(--s-btn-text); border-color: var(--s-btn); }
/* The scheme's OWN secondary-button colours, not a stand-in for them.
   This said `transparent / var(--s-heading) / var(--s-border)`, so the
   two Secondary button controls in the section panel changed nothing at
   all, and on a light scheme the outline was drawn in the faint divider
   colour instead of the dark one the scheme asks for. */
.s__btn--secondary { background: var(--s-btn2); color: var(--s-btn2-text); border-color: var(--s-btn2-border); }
.s__note { margin: 16px 0 0; font-size: 13px; color: var(--s-para); opacity: 0.8; }

.rt { font-size: var(--section-text-size, 16px); line-height: var(--section-text-line-height, 1.7); color: var(--s-para); }
.rt > :first-child { margin-top: 0; } .rt > :last-child { margin-bottom: 0; }
.rt p { margin: 0 0 1em; }
.rt a { color: var(--s-heading); text-decoration: underline; text-underline-offset: 3px; }
.rt ul { margin: 0 0 1em; padding-left: 1.35em; }

.fo__in { display: flex; flex-direction: column; align-items: center; text-align: center; }
.fo__photo { width: 96px; height: 96px; border-radius: 999px; object-fit: cover; margin-bottom: 22px; }
.fo__h { margin-bottom: 18px; }
.fo__body { max-width: 38rem; text-align: left; }
.fo__sign { margin-top: 26px; height: 48px; width: auto; }
.fo__by { margin: 18px 0 0; display: flex; flex-wrap: wrap; gap: 8px; justify-content: center; }
.fo__name { font-size: 14px; font-weight: 600; color: var(--s-heading); }
.fo__role { font-size: 14px; color: var(--s-para); opacity: 0.85; }
.fo__role::before { content: "· "; }
</style>

<schema>
{
  "schema_version": 1,
  "type": "about_founder",
  "name": "Founder Story",
  "category": "about",
  "description": "One person, their picture and their words.",
  "version": "1.0.0",
  "icon": "user",
  "settings": [
    {
      "type": "header",
      "id": "hdr_content",
      "label": "Content"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Photo",
      "default": ""
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": ""
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Why I started this"
    },
    {
      "type": "richtext",
      "id": "body",
      "label": "Text",
      "default": "<p>I could not find a bag that held a laptop and did not look like a laptop bag, so I made one. Then a friend wanted one, and then her sister did.</p>"
    },
    {
      "type": "image_picker",
      "id": "signature",
      "label": "Signature",
      "default": ""
    },
    {
      "type": "text",
      "id": "name",
      "label": "Name",
      "default": "Ada Whitlock"
    },
    {
      "type": "text",
      "id": "role",
      "label": "Role",
      "default": "Founder"
    },
    {
      "type": "header",
      "id": "hdr_layout",
      "label": "Layout"
    },
    {
      "type": "select",
      "id": "max_width",
      "label": "Content width",
      "default": "medium",
      "options": [
        {
          "label": "Narrow",
          "value": "narrow"
        },
        {
          "label": "Medium",
          "value": "medium"
        },
        {
          "label": "Wide",
          "value": "wide"
        },
        {
          "label": "Full",
          "value": "full"
        }
      ]
    },
    {
      "type": "header",
      "id": "hdr_style",
      "label": "Style"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Colour scheme",
      "default": "white"
    },
    {
      "type": "header",
      "id": "hdr_spacing",
      "label": "Spacing"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Top padding",
      "default": 80,
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Bottom padding",
      "default": 80,
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px"
    }
  ],
  "presets": [
    {
      "name": "Founder Story"
    }
  ]
}
</schema>
