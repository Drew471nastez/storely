<template>
<section class="s ti ti--{{ settings.image_side }}" data-scheme="{{ settings.color_scheme }}" style="--s-bg: {{ scheme.bg }}; --s-text: {{ scheme.text }}; --s-heading: {{ scheme.heading }}; --s-para: {{ scheme.paragraph }}; --s-border: {{ scheme.border }}; --s-accent: {{ scheme.accent }}; --s-btn: {{ scheme.primary_btn_bg }}; --s-btn-text: {{ scheme.primary_btn_text }}; --s-btn2: {{ scheme.secondary_btn_bg }}; --s-btn2-text: {{ scheme.secondary_btn_text }}; --s-btn2-border: {{ scheme.secondary_btn_border }}; --s-radius: {{ theme.radius.medium }}px; --s-pt: {{ settings.padding_top }}px; --s-pb: {{ settings.padding_bottom }}px;">
  <div class="s__in s__in--{{ settings.max_width }} ti__in">
    <div class="ti__media ti__media--{{ settings.image_shape }}">
      {% if settings.image %}<img src="{{ settings.image | asset }}" alt="{{ settings.image_alt }}" loading="lazy" decoding="async">{% else %}<div class="ti__placeholder" aria-hidden="true"></div>{% endif %}
      {% if settings.caption %}<figcaption class="ti__caption">{{ settings.caption }}</figcaption>{% endif %}
    </div>
    <div class="ti__copy">
      {% if settings.eyebrow %}<p class="s__eyebrow">{{ settings.eyebrow }}</p>{% endif %}
      {% if settings.heading %}<h2 class="s__h rt__h">{{ settings.heading }}</h2>{% endif %}
      {% if settings.body %}<div class="rt">{{ settings.body | raw }}</div>{% endif %}
      {% if settings.button_text %}
        <div class="s__btns"><a class="s__btn s__btn--primary" href="{{ settings.button_url | default: '#' | url }}">{{ settings.button_text }}</a></div>
      {% endif %}
    </div>
  </div>
</section>
</template>

<style scoped>
.s { container-type: inline-size; background: var(--s-bg); color: var(--s-text); }
.s__in {
  margin: 0 auto;
  padding: calc(var(--s-pt) * var(--theme-spacing-scale, 1)) var(--theme-padding-x, 24px) calc(var(--s-pb) * var(--theme-spacing-scale, 1));
}
.s__in--narrow { max-width: 720px; }
.s__in--medium { max-width: 960px; }
.s__in--wide { max-width: var(--theme-page-width, 1280px); }
.s__in--full { max-width: none; }
.s__eyebrow { margin: 0 0 12px; font-size: 12px; font-weight: 600; letter-spacing: 0.14em; text-transform: uppercase; color: var(--s-accent); }
.s__h {
  margin: 0;
  /* The section's heading style: Heading 2 unless the section picked
     another, set by the theme stylesheet on the section root. */
  font-size: var(--section-heading-size, 32px);
  line-height: var(--section-heading-line-height, 1.15);
  font-weight: var(--section-heading-weight, 700);
  color: var(--s-heading);
  letter-spacing: var(--section-heading-tracking, -0.01em);
  text-transform: var(--section-heading-transform, none);
  /* The theme's font when the page sets one, the token otherwise, and a real
     stack either way: a bare family name falls back to the browser's serif. */
  font-family: var(--theme-font-heading, {{ theme.typography.heading }}), ui-sans-serif, system-ui, sans-serif;
}
.s__sub { margin: 14px 0 0; font-size: var(--section-text-size, 17px); line-height: var(--section-text-line-height, 1.6); color: var(--s-para); }
.s__head { max-width: 44rem; }
.s__head--center { margin-inline: auto; text-align: center; }
.s__btns { display: flex; flex-wrap: wrap; gap: 12px; margin-top: 28px; }
.s__head--center .s__btns { justify-content: center; }
.s__btn {
  display: inline-flex; align-items: center; justify-content: center;
  padding: 13px 24px; font-size: 15px; font-weight: 600; text-decoration: none;
  border-radius: var(--s-radius); border: 1px solid transparent; transition: opacity 150ms ease;
}
.s__btn:hover { opacity: 0.88; }
.s__btn--primary { background: var(--s-btn); color: var(--s-btn-text); border-color: var(--s-btn); }
/* The scheme's OWN secondary-button colours, not a stand-in for them.
   This said `transparent / var(--s-heading) / var(--s-border)`, so the
   two Secondary button controls in the section panel changed nothing at
   all, and on a light scheme the outline was drawn in the faint divider
   colour instead of the dark one the scheme asks for. */
.s__btn--secondary { background: var(--s-btn2); color: var(--s-btn2-text); border-color: var(--s-btn2-border); }
.s__note { margin: 16px 0 0; font-size: 13px; color: var(--s-para); opacity: 0.8; }

.rt { font-size: var(--section-text-size, 16px); line-height: var(--section-text-line-height, 1.7); color: var(--s-para); }
.rt > :first-child { margin-top: 0; }
.rt > :last-child { margin-bottom: 0; }
.rt h2, .rt h3 { color: var(--s-heading); line-height: 1.25; margin: 1.6em 0 0.5em; }
.rt h2 { font-size: 24px; } .rt h3 { font-size: 19px; }
.rt p { margin: 0 0 1em; }
.rt a { color: var(--s-heading); text-decoration: underline; text-underline-offset: 3px; }
.rt ul, .rt ol { margin: 0 0 1em; padding-left: 1.35em; }
.rt li { margin: 0.35em 0; }
.rt blockquote { margin: 1.4em 0; padding-left: 18px; border-left: 3px solid var(--s-accent); color: var(--s-heading); font-size: 18px; }
.rt img { max-width: 100%; height: auto; border-radius: var(--s-radius); }

.rt__h { margin-bottom: 16px; }
.ti__in { display: grid; gap: 36px; align-items: center; grid-template-columns: 1fr; }
@container (min-width: 860px) { .ti__in { grid-template-columns: 1fr 1fr; gap: 56px; } .ti--right .ti__media { order: 2; } }
.ti__media { margin: 0; overflow: hidden; border-radius: var(--s-radius); }
.ti__media--square { border-radius: 0; }
.ti__media--round { border-radius: 24px; }
.ti__media img { display: block; width: 100%; object-fit: cover; aspect-ratio: 4 / 3; }
.ti__placeholder { width: 100%; aspect-ratio: 4 / 3; background: var(--s-border); }
.ti__caption { margin-top: 10px; font-size: 13px; color: var(--s-para); opacity: 0.8; }
.ti__copy { max-width: 34rem; }
</style>

<schema>
{
  "schema_version": 1,
  "type": "text_image",
  "name": "Image + Text",
  "category": "content",
  "description": "A picture beside a paragraph.",
  "version": "1.0.0",
  "icon": "image",
  "settings": [
    {
      "type": "header",
      "id": "hdr_content",
      "label": "Content"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": ""
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Where it is all made"
    },
    {
      "type": "richtext",
      "id": "body",
      "label": "Text",
      "default": "<p>A converted garage, two benches and a kettle that is always on. Everything you see was cut, sewn and packed here.</p>"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button",
      "default": ""
    },
    {
      "type": "url",
      "id": "button_url",
      "label": "Button link",
      "default": ""
    },
    {
      "type": "header",
      "id": "hdr_image",
      "label": "Image"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image",
      "default": ""
    },
    {
      "type": "text",
      "id": "image_alt",
      "label": "Image description",
      "default": ""
    },
    {
      "type": "text",
      "id": "caption",
      "label": "Caption",
      "default": ""
    },
    {
      "type": "radio",
      "id": "image_side",
      "label": "Image on",
      "default": "left",
      "options": [
        {
          "label": "Left",
          "value": "left"
        },
        {
          "label": "Right",
          "value": "right"
        }
      ]
    },
    {
      "type": "select",
      "id": "image_shape",
      "label": "Corners",
      "default": "default",
      "options": [
        {
          "label": "Theme",
          "value": "default"
        },
        {
          "label": "Rounded",
          "value": "round"
        },
        {
          "label": "Square",
          "value": "square"
        }
      ]
    },
    {
      "type": "header",
      "id": "hdr_layout",
      "label": "Layout"
    },
    {
      "type": "select",
      "id": "max_width",
      "label": "Content width",
      "default": "wide",
      "options": [
        {
          "label": "Narrow",
          "value": "narrow"
        },
        {
          "label": "Medium",
          "value": "medium"
        },
        {
          "label": "Wide",
          "value": "wide"
        },
        {
          "label": "Full",
          "value": "full"
        }
      ]
    },
    {
      "type": "header",
      "id": "hdr_style",
      "label": "Style"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Colour scheme",
      "default": "white"
    },
    {
      "type": "header",
      "id": "hdr_spacing",
      "label": "Spacing"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Top padding",
      "default": 72,
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Bottom padding",
      "default": 72,
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px"
    }
  ],
  "presets": [
    {
      "name": "Image + Text"
    },
    {
      "name": "Image + Text, image right",
      "settings": {
        "image_side": "right"
      }
    }
  ]
}
</schema>
