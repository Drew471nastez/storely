<template>
<section class="lda" data-scheme="{{ settings.color_scheme }}" style="--bg:{{ settings.background }};--ink:{{ settings.text }};--muted:{{ settings.muted }};--line:{{ settings.border }};">
  <div class="lda__in">
    <img src="{{ settings.logo_url | asset }}" alt="{{ settings.logo_alt }}" width="164" height="164" loading="lazy">
    <div><h2>About me</h2><p>{{ settings.biography }}</p><p>{{ settings.note }}</p></div>
  </div>
</section>
</template>

<style scoped>
.lda { min-height: 390px; border-top: 1px solid var(--line); border-bottom: 1px solid var(--line); background: var(--bg); color: var(--ink); font-family: Arial, Helvetica, sans-serif; }
.lda__in { display: grid; grid-template-columns: 164px minmax(0, 620px); width: min(930px, calc(100% - 64px)); min-height: 390px; margin: 0 auto; align-items: center; gap: 52px; }
.lda img { width: 164px; height: 164px; object-fit: cover; }
.lda h2 { margin: 0 0 22px; font-family: "Bodoni 72", Didot, Georgia, serif; font-size: 52px; font-weight: 400; letter-spacing: -2.2px; line-height: 1; }
.lda p { margin: 0 0 14px; color: var(--muted); font-size: 14px; line-height: 1.6; }
@container (max-width: 720px) { .lda__in { grid-template-columns: 110px 1fr; width: calc(100% - 32px); min-height: 440px; gap: 28px; } .lda img { width: 110px; height: 110px; } .lda h2 { font-size: 44px; } .lda p { font-size: 15px; } }
</style>

<schema>
{
  "schema_version": 1,
  "type": "ld_08_about",
  "name": "LD 08 — About",
  "category": "custom",
  "version": "1.0.1",
  "settings": [
    { "type": "url", "id": "logo_url", "label": "Logo URL", "default": "https://lucadragoste.ro/logo-ld.png" },
    { "type": "text", "id": "logo_alt", "label": "Logo alt text", "default": "Luca Dragoste LD logo" },
    { "type": "textarea", "id": "biography", "label": "Biography", "default": "I’m Luca, an Advertising student at SNSPA in Bucharest. I run both services myself, so the person you speak with also does the research and writing." },
    { "type": "textarea", "id": "note", "label": "Note", "default": "I keep the work small, direct, and easy to review." },
    { "type": "color_scheme", "id": "color_scheme", "label": "Base colour scheme", "default": "white" },
    { "type": "color", "id": "background", "label": "Background", "default": "#FFFFFF" },
    { "type": "color", "id": "text", "label": "Text", "default": "#1A1D24" },
    { "type": "color", "id": "muted", "label": "Muted text", "default": "#4E5156" },
    { "type": "color", "id": "border", "label": "Borders", "default": "#BDC0C5" }
  ],
  "presets": [{ "name": "LD 08 — About" }]
}
</schema>
