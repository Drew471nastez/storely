<template>
<section class="s" data-scheme="{{ settings.color_scheme }}" style="--s-bg: {{ scheme.bg }}; --s-text: {{ scheme.text }}; --s-heading: {{ scheme.heading }}; --s-para: {{ scheme.paragraph }}; --s-border: {{ scheme.border }}; --s-accent: {{ scheme.accent }}; --s-btn: {{ scheme.primary_btn_bg }}; --s-btn-text: {{ scheme.primary_btn_text }}; --s-btn2: {{ scheme.secondary_btn_bg }}; --s-btn2-text: {{ scheme.secondary_btn_text }}; --s-btn2-border: {{ scheme.secondary_btn_border }}; --s-radius: {{ theme.radius.medium }}px; --s-pt: {{ settings.padding_top }}px; --s-pb: {{ settings.padding_bottom }}px;">
  <div class="s__in s__in--{{ settings.max_width }}">
    {% if settings.heading %}<p class="lr__title">{{ settings.heading }}</p>{% endif %}
    {% if blocks.size == 0 %}
      {% if page.in_editor %}<p class="empty">Add logos in the sidebar</p>{% endif %}
    {% else %}
      <ul class="lr" style="--lr-op: {{ settings.opacity | divided_by: 100 }}; --lr-h: {{ settings.logo_height }}px;">
        {% for block in blocks %}
          <li data-block-id="{{ block.id }}">
            {% if block.settings.image %}
              {% if block.settings.url %}<a href="{{ block.settings.url | url }}"><img src="{{ block.settings.image | asset }}" alt="{{ block.settings.name }}" loading="lazy" decoding="async"></a>
              {% else %}<img src="{{ block.settings.image | asset }}" alt="{{ block.settings.name }}" loading="lazy" decoding="async">{% endif %}
            {% else %}
              <span class="lr__word">{{ block.settings.name }}</span>
            {% endif %}
          </li>
        {% endfor %}
      </ul>
    {% endif %}
  </div>
</section>
</template>

<style scoped>
.s { container-type: inline-size; background: var(--s-bg); color: var(--s-text); }
.s__in {
  margin: 0 auto;
  padding: calc(var(--s-pt) * var(--theme-spacing-scale, 1)) var(--theme-padding-x, 24px) calc(var(--s-pb) * var(--theme-spacing-scale, 1));
}
.s__in--narrow { max-width: 720px; }
.s__in--medium { max-width: 960px; }
.s__in--wide { max-width: var(--theme-page-width, 1280px); }
.s__in--full { max-width: none; }
.s__eyebrow { margin: 0 0 12px; font-size: 12px; font-weight: 600; letter-spacing: 0.14em; text-transform: uppercase; color: var(--s-accent); }
.s__h {
  margin: 0;
  /* The section's heading style: Heading 2 unless the section picked
     another, set by the theme stylesheet on the section root. */
  font-size: var(--section-heading-size, 32px);
  line-height: var(--section-heading-line-height, 1.15);
  font-weight: var(--section-heading-weight, 700);
  color: var(--s-heading);
  letter-spacing: var(--section-heading-tracking, -0.01em);
  text-transform: var(--section-heading-transform, none);
  /* The theme's font when the page sets one, the token otherwise, and a real
     stack either way: a bare family name falls back to the browser's serif. */
  font-family: var(--theme-font-heading, {{ theme.typography.heading }}), ui-sans-serif, system-ui, sans-serif;
}
.s__sub { margin: 14px 0 0; font-size: var(--section-text-size, 17px); line-height: var(--section-text-line-height, 1.6); color: var(--s-para); }
.s__head { max-width: 44rem; }
.s__head--center { margin-inline: auto; text-align: center; }
.s__btns { display: flex; flex-wrap: wrap; gap: 12px; margin-top: 28px; }
.s__head--center .s__btns { justify-content: center; }
.s__btn {
  display: inline-flex; align-items: center; justify-content: center;
  padding: 13px 24px; font-size: 15px; font-weight: 600; text-decoration: none;
  border-radius: var(--s-radius); border: 1px solid transparent; transition: opacity 150ms ease;
}
.s__btn:hover { opacity: 0.88; }
.s__btn--primary { background: var(--s-btn); color: var(--s-btn-text); border-color: var(--s-btn); }
/* The scheme's OWN secondary-button colours, not a stand-in for them.
   This said `transparent / var(--s-heading) / var(--s-border)`, so the
   two Secondary button controls in the section panel changed nothing at
   all, and on a light scheme the outline was drawn in the faint divider
   colour instead of the dark one the scheme asks for. */
.s__btn--secondary { background: var(--s-btn2); color: var(--s-btn2-text); border-color: var(--s-btn2-border); }
.s__note { margin: 16px 0 0; font-size: 13px; color: var(--s-para); opacity: 0.8; }

.lr__title { margin: 0 0 22px; text-align: center; font-size: 12px; font-weight: 600; letter-spacing: 0.14em; text-transform: uppercase; color: var(--s-para); opacity: 0.75; }
.lr { margin: 0; padding: 0; list-style: none; display: flex; flex-wrap: wrap; align-items: center; justify-content: center; gap: 28px 44px; }
/* The height was fixed and the width was not, so an unloaded logo took no
   width at all and the row packed tight: once the pictures arrived it
   rewrapped, measured at +56px on a phone. The ratio reserves a plausible
   width until the real one is known, after which the logo's own ratio wins
   because the width is still auto. */
.lr img {
  display: block;
  height: var(--lr-h);
  width: auto;
  aspect-ratio: 3 / 1;
  max-width: 160px;
  object-fit: contain;
  opacity: var(--lr-op);
}
.lr__word { font-size: 17px; font-weight: 600; letter-spacing: 0.01em; color: var(--s-heading); opacity: var(--lr-op); }
.empty { margin: 0; text-align: center; font-size: 14px; color: var(--s-para); opacity: 0.55; }
</style>

<schema>
{
  "schema_version": 1,
  "type": "proof_logos",
  "name": "Logo / Trust Row",
  "category": "social-proof",
  "description": "A quiet row of logos or short claims.",
  "version": "1.0.0",
  "icon": "award",
  "settings": [
    {
      "type": "header",
      "id": "hdr_content",
      "label": "Content"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Line above",
      "default": "Stocked by"
    },
    {
      "type": "header",
      "id": "hdr_layout",
      "label": "Layout"
    },
    {
      "type": "range",
      "id": "logo_height",
      "label": "Logo height",
      "default": 28,
      "min": 16,
      "max": 64,
      "step": 2,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "opacity",
      "label": "Strength",
      "default": 70,
      "min": 20,
      "max": 100,
      "step": 5,
      "unit": "%"
    },
    {
      "type": "select",
      "id": "max_width",
      "label": "Content width",
      "default": "medium",
      "options": [
        {
          "label": "Narrow",
          "value": "narrow"
        },
        {
          "label": "Medium",
          "value": "medium"
        },
        {
          "label": "Wide",
          "value": "wide"
        },
        {
          "label": "Full",
          "value": "full"
        }
      ]
    },
    {
      "type": "header",
      "id": "hdr_style",
      "label": "Style"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Colour scheme",
      "default": "white"
    },
    {
      "type": "header",
      "id": "hdr_spacing",
      "label": "Spacing"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Top padding",
      "default": 48,
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Bottom padding",
      "default": 48,
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px"
    }
  ],
  "blocks": [
    {
      "type": "logo",
      "name": "Logo",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Logo",
          "default": ""
        },
        {
          "type": "text",
          "id": "name",
          "label": "Name",
          "default": "A shop",
          "info": "Shown as words when there is no logo, and read aloud when there is."
        },
        {
          "type": "url",
          "id": "url",
          "label": "Link",
          "default": ""
        }
      ]
    }
  ],
  "max_blocks": 12,
  "presets": [
    {
      "name": "Logo / Trust Row",
      "settings": {},
      "blocks": [
        {
          "id": "b1",
          "type": "logo",
          "settings": {
            "name": "Goods & Co"
          }
        },
        {
          "id": "b2",
          "type": "logo",
          "settings": {
            "name": "The Corner Shop"
          }
        },
        {
          "id": "b3",
          "type": "logo",
          "settings": {
            "name": "Pine Market"
          }
        },
        {
          "id": "b4",
          "type": "logo",
          "settings": {
            "name": "Ridley's"
          }
        },
        {
          "id": "b5",
          "type": "logo",
          "settings": {
            "name": "Hollow Lane"
          }
        }
      ]
    }
  ]
}
</schema>
